spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/guaijie/config-center
          skip-ssl-validation: true
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://user:user@localhost:8761/eureka/