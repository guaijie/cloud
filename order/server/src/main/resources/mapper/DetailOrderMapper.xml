<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.server.dao.DetailOrderDao">
    <!--<resultMap id="addUserOrder" type="com.clown.design.entity.Role">-->
    <!--<id column="id" property="id"/>-->
    <!--<result column="roleName" property="roleName"/>-->
    <!--<result column="password" property="password"/>-->
    <!--<result column="roleType" property="roleType"/>-->
    <!--</resultMap>-->
    <insert id="addOrderDetail" parameterType="com.example.order.server.entity.DetailOrder">
        INSERT INTO order_detail(id,order_id,product_id,product_name,product_price,product_counts,category_name,category_type)
        VALUES (#{id},#{orderId},#{productId},#{productName},#{productPrice},#{productCounts},#{categoryName},#{categoryType})
    </insert>

    <!--<select id="selectRoleByPassWord" parameterType="Map" resultType="com.clown.design.entity.Role">-->
    <!--SELECT * FROM role WHERE roleName = #{roleName} AND passWord = #{passWord}-->
    <!--</select>-->

</mapper>